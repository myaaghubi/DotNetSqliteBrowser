<UserControl x:Class="DotNetSqliteBrowser.tableObject"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="200" d:DesignWidth="200" Width="200" Height="200" Margin="5">
    <Grid Background="#FFD6E1FF" >

        <TextBlock x:Name="tablename_txt" Margin="50,2,50,0" TextWrapping="Wrap" Text="..." VerticalAlignment="Top" TextAlignment="Center" FontSize="10"/>
        <Button x:Name="close_btn" Content="X" HorizontalAlignment="Left" Margin="3,2,0,0" VerticalAlignment="Top" Width="19" Click="close_btn_Click" FontSize="9"/>
        <DataGrid Margin="3,20, 3, 3" Width="Auto" Background="White" IsReadOnly="True" FontSize="10">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Binding="{x:Null}" ClipboardContentBinding="{x:Null}"/>
                <DataGridTextColumn Binding="{x:Null}" ClipboardContentBinding="{x:Null}" Header="Column"/>
                <DataGridTextColumn Binding="{x:Null}" ClipboardContentBinding="{x:Null}" Header="Type"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</UserControl>
